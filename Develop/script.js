// date in the header
function getTodayDate() {
var today = moment().format("dddd, MMMM Do");
$("#currentDay").text(today);
}

// loads today's date
getTodayDate();

// local storage


// save
function save() {

}

// json 